version: '3.8'
services:
  lab-037:
    image: ubuntu:20.04
    container_name: day-037-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=37
      - LAB_TITLE="Copy File to Docker Container"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
