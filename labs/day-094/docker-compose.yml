version: '3.8'
services:
  lab-094:
    image: ubuntu:20.04
    container_name: day-094-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=94
      - LAB_TITLE="Terraform: VPC"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
