version: '3.8'
services:
  lab-078:
    image: ubuntu:20.04
    container_name: day-078-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=78
      - LAB_TITLE="Jenkins: Conditional Pipeline"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
