version: '3.8'
services:
  lab-083:
    image: ubuntu:20.04
    container_name: day-083-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=83
      - LAB_TITLE="Ansible: Troubleshoot & Playbook"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
