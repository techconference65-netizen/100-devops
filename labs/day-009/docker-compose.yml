version: '3.8'
services:
  lab-009:
    image: ubuntu:20.04
    container_name: day-009-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=9
      - LAB_TITLE="MariaDB Troubleshooting"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
