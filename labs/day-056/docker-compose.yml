version: '3.8'
services:
  lab-056:
    image: ubuntu:20.04
    container_name: day-056-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=56
      - LAB_TITLE="K8s: Nginx Web Server"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
