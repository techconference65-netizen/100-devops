version: '3.8'
services:
  lab-057:
    image: ubuntu:20.04
    container_name: day-057-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=57
      - LAB_TITLE="K8s: Env Vars"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
