version: '3.8'
services:
  lab-050:
    image: ubuntu:20.04
    container_name: day-050-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=50
      - LAB_TITLE="K8s: Resource Limits"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
