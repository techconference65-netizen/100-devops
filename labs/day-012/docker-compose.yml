version: '3.8'
services:
  lab-012:
    image: ubuntu:20.04
    container_name: day-012-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=12
      - LAB_TITLE="Linux Network Services"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
