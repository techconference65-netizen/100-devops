version: '3.8'
services:
  lab-018:
    image: ubuntu:20.04
    container_name: day-018-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=18
      - LAB_TITLE="Configure LAMP Server"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
