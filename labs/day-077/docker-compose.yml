version: '3.8'
services:
  lab-077:
    image: ubuntu:20.04
    container_name: day-077-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=77
      - LAB_TITLE="Jenkins: Deploy Pipeline"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
