version: '3.8'
services:
  lab-008:
    image: ubuntu:20.04
    container_name: day-008-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=8
      - LAB_TITLE="Install Ansible"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
