version: '3.8'
services:
  lab-043:
    image: ubuntu:20.04
    container_name: day-043-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=43
      - LAB_TITLE="Docker Port Mapping"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
