version: '3.8'
services:
  lab-088:
    image: ubuntu:20.04
    container_name: day-088-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=88
      - LAB_TITLE="Ansible: Blockinfile"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
