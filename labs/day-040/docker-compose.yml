version: '3.8'
services:
  lab-040:
    image: ubuntu:20.04
    container_name: day-040-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=40
      - LAB_TITLE="Docker Exec Operations"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
