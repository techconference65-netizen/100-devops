version: '3.8'
services:
  lab-055:
    image: ubuntu:20.04
    container_name: day-055-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=55
      - LAB_TITLE="K8s: Sidecars"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
