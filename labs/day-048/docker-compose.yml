version: '3.8'
services:
  lab-048:
    image: ubuntu:20.04
    container_name: day-048-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=48
      - LAB_TITLE="K8s: Deploy Pods"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
