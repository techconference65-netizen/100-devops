version: '3.8'
services:
  lab-096:
    image: ubuntu:20.04
    container_name: day-096-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=96
      - LAB_TITLE="Terraform: EC2 Instance"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
