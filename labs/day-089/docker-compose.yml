version: '3.8'
services:
  lab-089:
    image: ubuntu:20.04
    container_name: day-089-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=89
      - LAB_TITLE="Ansible: Manage Services"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
