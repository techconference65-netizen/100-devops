version: '3.8'
services:
  lab-042:
    image: ubuntu:20.04
    container_name: day-042-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=42
      - LAB_TITLE="Create a Docker Network"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
