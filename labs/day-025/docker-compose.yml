version: '3.8'
services:
  lab-025:
    image: ubuntu:20.04
    container_name: day-025-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=25
      - LAB_TITLE="Git: Merge Branches"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
