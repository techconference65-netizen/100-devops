version: '3.8'
services:
  lab-022:
    image: ubuntu:20.04
    container_name: day-022-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=22
      - LAB_TITLE="Clone Git Repository on Storage Server"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
