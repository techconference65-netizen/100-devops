version: '3.8'
services:
  lab-068:
    image: ubuntu:20.04
    container_name: day-068-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=68
      - LAB_TITLE="Jenkins: Server Setup"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
