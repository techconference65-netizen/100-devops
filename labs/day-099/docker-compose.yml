version: '3.8'
services:
  lab-099:
    image: ubuntu:20.04
    container_name: day-099-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=99
      - LAB_TITLE="Terraform: Attach Policy for DynamoDB"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
