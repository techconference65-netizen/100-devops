version: '3.8'
services:
  lab-063:
    image: ubuntu:20.04
    container_name: day-063-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=63
      - LAB_TITLE="K8s: Iron Gallery App"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
