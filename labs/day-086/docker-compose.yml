version: '3.8'
services:
  lab-086:
    image: ubuntu:20.04
    container_name: day-086-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=86
      - LAB_TITLE="Ansible: Ping Module"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
