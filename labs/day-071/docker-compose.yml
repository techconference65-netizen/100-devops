version: '3.8'
services:
  lab-071:
    image: ubuntu:20.04
    container_name: day-071-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=71
      - LAB_TITLE="Jenkins: Package Install Job"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
