version: '3.8'
services:
  lab-051:
    image: ubuntu:20.04
    container_name: day-051-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=51
      - LAB_TITLE="K8s: Rolling Updates"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
