version: '3.8'
services:
  lab-098:
    image: ubuntu:20.04
    container_name: day-098-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=98
      - LAB_TITLE="Terraform: EC2 in Private Subnet"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
