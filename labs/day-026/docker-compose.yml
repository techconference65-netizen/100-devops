version: '3.8'
services:
  lab-026:
    image: ubuntu:20.04
    container_name: day-026-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=26
      - LAB_TITLE="Git: Manage Remotes"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
