version: '3.8'
services:
  lab-005:
    image: ubuntu:20.04
    container_name: day-005-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=5
      - LAB_TITLE="SELinux Installation and Configuration"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
