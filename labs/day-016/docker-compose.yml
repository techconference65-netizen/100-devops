version: '3.8'
services:
  lab-016:
    image: ubuntu:20.04
    container_name: day-016-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=16
      - LAB_TITLE="Nginx as a Load Balancer"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
