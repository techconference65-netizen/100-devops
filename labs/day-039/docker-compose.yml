version: '3.8'
services:
  lab-039:
    image: ubuntu:20.04
    container_name: day-039-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=39
      - LAB_TITLE="Commit a Container as Image"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
