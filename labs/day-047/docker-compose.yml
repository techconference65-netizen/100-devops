version: '3.8'
services:
  lab-047:
    image: ubuntu:20.04
    container_name: day-047-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=47
      - LAB_TITLE="Dockerized Python App"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
