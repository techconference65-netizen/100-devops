version: '3.8'
services:
  lab-027:
    image: ubuntu:20.04
    container_name: day-027-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=27
      - LAB_TITLE="Git: Revert Some Changes"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
