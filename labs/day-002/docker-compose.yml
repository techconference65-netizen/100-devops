version: '3.8'
services:
  lab-002:
    image: ubuntu:20.04
    container_name: day-002-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=2
      - LAB_TITLE="Temporary User Setup with Expiry"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
