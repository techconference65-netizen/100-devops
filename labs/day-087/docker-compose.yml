version: '3.8'
services:
  lab-087:
    image: ubuntu:20.04
    container_name: day-087-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=87
      - LAB_TITLE="Ansible: Install Package"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
