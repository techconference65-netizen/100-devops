version: '3.8'
services:
  lab-061:
    image: ubuntu:20.04
    container_name: day-061-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=61
      - LAB_TITLE="K8s: Init Containers"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
