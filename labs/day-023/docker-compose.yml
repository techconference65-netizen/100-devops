version: '3.8'
services:
  lab-023:
    image: ubuntu:20.04
    container_name: day-023-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=23
      - LAB_TITLE="Fork a Git Repository"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
