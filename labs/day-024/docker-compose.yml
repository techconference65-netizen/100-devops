version: '3.8'
services:
  lab-024:
    image: ubuntu:20.04
    container_name: day-024-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=24
      - LAB_TITLE="Git: Create Branches"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
