version: '3.8'
services:
  lab-076:
    image: ubuntu:20.04
    container_name: day-076-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=76
      - LAB_TITLE="Jenkins: Project Security"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
