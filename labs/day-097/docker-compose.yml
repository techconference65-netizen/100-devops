version: '3.8'
services:
  lab-097:
    image: ubuntu:20.04
    container_name: day-097-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=97
      - LAB_TITLE="Terraform: IAM Policy"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
