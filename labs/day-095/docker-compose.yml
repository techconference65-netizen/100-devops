version: '3.8'
services:
  lab-095:
    image: ubuntu:20.04
    container_name: day-095-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=95
      - LAB_TITLE="Terraform: Security Group"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
