version: '3.8'
services:
  lab-079:
    image: ubuntu:20.04
    container_name: day-079-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=79
      - LAB_TITLE="Jenkins: Deployment Job"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
