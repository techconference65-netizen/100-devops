version: '3.8'
services:
  lab-064:
    image: ubuntu:20.04
    container_name: day-064-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=64
      - LAB_TITLE="K8s: Fix Python App"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
