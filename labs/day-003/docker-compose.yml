version: '3.8'
services:
  lab-003:
    image: ubuntu:20.04
    container_name: day-003-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=3
      - LAB_TITLE="Secure Root SSH Access"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
