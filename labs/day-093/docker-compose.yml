version: '3.8'
services:
  lab-093:
    image: ubuntu:20.04
    container_name: day-093-lab
    command: tail -f /dev/null
    volumes:
      - ./workspace:/workspace
    environment:
      - LAB_DAY=93
      - LAB_TITLE="Ansible: Conditionals"
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
